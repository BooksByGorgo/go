---
title: "Gorgo Go for Java Programmers"
toc: true
toc-depth: 1
colorlinks: true
numbersections: true
secnumdepth: 0
mainfont: "TeX Gyre Pagella"
monofont: "JetBrains Mono"
header-includes:
  - \usepackage[most]{tcolorbox}
  - \usepackage{makeidx}
  - \makeindex
  - \usepackage{microtype}
  - \setlength{\emergencystretch}{3em}
  - \setcounter{chapter}{-1}
  - \AtBeginDocument{\pagenumbering{gobble}}
  - |
    ```{=latex}
    % the 7x10 lulu trim gives a 5.5in text block; body code lines run up
    % to 96 monospace characters, which only fit at \scriptsize (8pt).
    \usepackage{fancyvrb}
    \fvset{fontsize=\scriptsize}
    \makeatletter
    \def\verbatim@font{\ttfamily\scriptsize}
    \makeatother
    ```
  - |
    ```{=latex}
    \usepackage{fancyhdr}
    \pagestyle{fancy}
    \fancyhf{}
    \fancyfoot[LE,RO]{\thepage}
    \fancyfoot[CE,CO]{\nouppercase{\leftmark}}
    \renewcommand{\headrulewidth}{0pt}
    \fancypagestyle{plain}{%
      \fancyhf{}%
      \fancyfoot[LE,RO]{\thepage}%
      \fancyfoot[CE,CO]{\nouppercase{\leftmark}}%
      \renewcommand{\headrulewidth}{0pt}%
    }
    \makeatletter
    \let\gorgoOrigChapter\chapter
    \def\chapter{\@ifstar\gorgoStarChapter\gorgoOrigChapter}
    \newcommand\gorgoStarChapter[1]{\gorgoOrigChapter*{#1}\markboth{#1}{#1}}
    \makeatother
    ```
  - \usepackage{titling}
  - \pretitle{\begin{center}\includegraphics[width=0.35\textwidth]{../images/gorgo-g4j.png}\par\vspace{1em}\LARGE}
  - \posttitle{\par\end{center}\vskip 0.5em}
  - |
    ```{=latex}
    \directlua{luaotfload.add_fallback("main_fallback", {
      "NotoSerifCJKJP:mode=harf;",
      "DejaVuSerif:mode=harf;",
      "NotoColorEmoji:mode=harf;",
    })}
    \directlua{luaotfload.add_fallback("mono_fallback", {
      "NotoSansMonoCJKJP:mode=harf;",
      "DejaVuSansMono:mode=harf;",
      "NotoColorEmoji:mode=harf;",
    })}
    \setmainfont{TeX Gyre Pagella}[RawFeature={fallback=main_fallback}]
    \setmonofont{JetBrains Mono}[Scale=MatchLowercase, RawFeature={-calt,-liga,-dlig,fallback=mono_fallback}]
    ```
include-before:
  - \clearpage
  - |
    ```{=latex}
    \thispagestyle{empty}
    \vspace*{\fill}
    \begin{center}
    dear reader, we hope this book makes the jump from Java to Go a
    pleasant one.

    \medskip
    this is an open source book.
    if you find something that can be improved, please take a moment to go to
    \url{https://gorgo.dev/go} and open an issue. we will try to fix it.

    \medskip
    \textbf{may the source be with you! always}
    \end{center}
    \vspace*{\fill}
    \clearpage
    ```
  - \pagenumbering{roman}
---
